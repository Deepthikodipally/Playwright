import { test, expect } from '@playwright/test';

// test('test', async ({ page }) => {  
//   await page.goto('https://www.npmjs.com/package/playwright');
//   await page.getByRole('tab', { name: 'Code Beta' }).click();
//   await page.getByRole('tab', { name: 'Dependency' }).click();
//   await page.getByRole('tab', { name: 'Dependents' }).click();
//   await page.getByRole('tab', { name: 'Versions' }).click();
//   await page.getByRole('button', { name: 'Copy install command line' }).click();
// });



// h5 
//[placeholder='Username']
//[type='password']
//button or [type='submit']

